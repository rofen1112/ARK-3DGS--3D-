# ARK Gaussian Data Contract

Status: Phase 1 active. The browser now consumes the bundled scene data report for default camera fitting.

This document defines the first internal data contract for ARK-3DGS Gaussian scenes. The goal is to let ARK understand the source data before the first-party renderer is built.

## Current Supported Source

Current first-party parser target:

- Format: binary little-endian Gaussian PLY 1.0
- Source file: `public/scenes/demo_room_001/gaussian/scene.ply`
- Validation report: `public/scenes/demo_room_001/meta/gaussian_data_contract_report.json`
- Deterministic preview file: `public/scenes/demo_room_001/gaussian/scene-preview-100k.ply`
- Preview report: `public/scenes/demo_room_001/meta/ply_preview_report.json`

Validation command:

```bash
npm.cmd run validate:ply
```

Parser regression command:

```bash
npm.cmd run test:gaussian
```

Preview generation command:

```bash
npm.cmd run prepare:ply-preview
```

Manifest validation command:

```bash
npm.cmd run validate:manifest
```

Tiny regression fixture:

```text
public/scenes/fixtures/tiny_gaussian_invalid.ply
```

## Required PLY Fields

The parser currently requires:

```text
x, y, z
f_dc_0, f_dc_1, f_dc_2
opacity
scale_0, scale_1, scale_2
rot_0, rot_1, rot_2, rot_3
```

Optional SH fields:

```text
f_rest_0 ... f_rest_N
```

## Internal Encoding

ARK records the raw training/export encoding explicitly:

| Field | ARK Encoding | Notes |
|---|---|---|
| center | `float32_xyz` | 3D Gaussian center |
| colorDc | `spherical_harmonics_dc_rgb` | SH DC coefficients, not final sRGB |
| opacity | `logit` | Renderer must apply sigmoid |
| scale | `log_xyz` | Renderer must apply exp |
| rotation | `quaternion_wxyz` | Standard GraphDECO-style PLY order |
| shRest | `spherical_harmonics_rest_rgb` | Higher-order SH terms |

## Current Asset Summary

From the first validation report:

| Metric | Value |
|---|---:|
| Vertex / splat count | `1,855,266` |
| Valid positions | `1,854,627` |
| Invalid positions | `639` |
| Position validity ratio | `0.99965557` |
| PLY properties | `62` |
| Vertex stride | `248` bytes |
| Header bytes | `1532` |
| SH degree | `3` |
| SH rest fields | `45` |

Exact finite-position bounds:

```json
{
  "min": [-4.609628, -6.477489, -5.598247],
  "max": [12.420171, 5.007942, 12.98222]
}
```

Dense percentile bounds from the current report:

| Bounds | Low / High | Min | Max | Use |
|---|---:|---|---|---|
| `broad_01_99` | `0.01 / 0.99` | `[-2.535121, -4.258497, -4.28949]` | `[4.863048, 1.211781, 9.86895]` | Default visual fit candidate |
| `solid_05_95` | `0.05 / 0.95` | `[-2.412613, -3.59334, -1.435366]` | `[2.395744, 1.135266, 7.139618]` | Stable scene body candidate |
| `core_10_90` | `0.10 / 0.90` | `[-2.333623, -2.557385, 0.213389]` | `[2.233782, 1.094724, 5.753465]` | Core density / QA candidate |

Renderer and camera-fit rule:

> Exact bounds are kept for audit. Runtime camera fitting should prefer percentile bounds because exact bounds can be dominated by sparse outliers.

Current runtime rule:

> The bundled browser scene uses `broad_01_99` from `gaussian_data_contract_report.json` as its fit bounds. Local imports still fall back to renderer-computed bounds until sidecar metadata is provided with the import package.

Current visual QA rule:

> The browser evaluates a runtime visual quality gate after load. It requires a loaded scene, non-zero canvas, resolved fit bounds, bundled sidecar bounds, visible pixel sample, and contrast greater than or equal to `12`.

Current bundled format matrix:

| Format | Splats | SH Degree | Visual QA | Contrast |
|---|---:|---:|---|---:|
| SOG | `1,855,266` | `0` | Passed | `114.333333` |
| SPZ | `1,855,266` | `0` | Passed | `116.111111` |
| PLY | `1,855,266` | `0` | Passed | `101.888889` |

The matrix is generated by:

```bash
npm.cmd run qa:formats
```

Current ARK vs direct Aholo baseline:

| Format | ARK Bounds | Baseline Bounds | Splat Delta | Max Bounds Delta | Interpretation |
|---|---|---|---:|---:|---|
| SOG | `broad_01_99 (sidecar)` | `sog_meta_dense (renderer)` | `0` | `7.231167` | Different fit strategy; both render. |
| SPZ | `broad_01_99 (sidecar)` | `aholo_dense_98 (renderer)` | `0` | `1.836224` | Different fit strategy; both render. |
| PLY | `broad_01_99 (sidecar)` | `aholo_dense_98 (renderer)` | `0` | `2.218992` | Different fit strategy; both render. |

The comparison is generated by:

```bash
npm.cmd run qa:baseline
```

## Independent Renderer Preview Baseline

The second independent renderer baseline uses `@mkkellogg/gaussian-splats-3d`, a Three.js Gaussian renderer with official support for PLY/SPLAT/KSPLAT.

The full source PLY is still validated by ARK and by the direct Aholo baseline. In headless Chrome SwiftShader, the independent renderer did not finish processing the 460MB full PLY within 5 minutes, so this baseline uses a deterministic same-schema preview PLY generated from the full source asset.

Preview asset summary:

| Metric | Value |
|---|---:|
| Source splats | `1,855,266` |
| Target preview splats | `100,000` |
| Selected preview splats | `99,966` |
| Invalid skipped while sampling | `34` |
| Output size | `24,793,098` bytes |
| Vertex stride | `248` bytes |
| Property count | `62` |

Current ARK vs independent GaussianSplats3D result:

| Asset | ARK QA | Independent QA | Splat Delta | Interpretation |
|---|---|---|---:|---|
| `scene-preview-100k.ply` | `Passed (128.9)` | `Passed (162.7)` | `0` | Both render the same preview PLY under `broad_01_99` sidecar fit bounds. |

The comparison is generated by:

```bash
npm.cmd run qa:independent
```

## First-Party Renderer Preview Baseline

ARK now includes a first-party diagnostic WebGL backend:

```text
src/sdk/ark/ArkPointRendererBackend.ts
```

This backend is intentionally limited: it renders decoded PLY centers as SH0-colored circular scale-aware points with CPU back-to-front sorting. It proves ARK-owned parsing, invalid-position filtering, decoded scale/opacity packing, GPU buffer upload, camera fit, canvas rendering, depth-order diagnostics, and visual QA without Aholo.

Current result:

| Renderer | Asset | Splats | Visual QA | Shader | Sorting | Limitation |
|---|---|---:|---|---|---|---|
| `ark-point-webgl2` | `scene-preview-100k.ply` | `99,966` | `Passed (216.0)` | `sh0-scale-aware-point-cloud` | `cpu-back-to-front` | Point renderer, not full Gaussian projection. |

The QA is generated by:

```bash
npm.cmd run qa:first-party
```

Open manually:

```text
http://127.0.0.1:5173/?autoload=1&asset=ply-preview&renderer=ark-point
```

ARK also includes the active first-party Gaussian WebGL2 backend:

```text
src/sdk/ark/ArkGaussianRendererBackend.ts
```

This backend uses decoded PLY centers, SH0 color, opacity, scale, and rotation quaternion to draw instanced quads with screen-space ellipse projection. It also applies explicit near/far and offscreen center clipping before fragment shading. It is the next renderer milestone after the diagnostic point backend.

The QA is generated by:

```bash
npm.cmd run qa:first-party-gaussian
```

Open manually:

```text
http://127.0.0.1:5173/?autoload=1&asset=ply-preview&renderer=ark-gaussian
```

Current result:

| Renderer | Asset | Splats | Visual QA | Shader | Sorting | Projection | Clipping |
|---|---|---:|---|---|---|---|---|
| `ark-gaussian-webgl2` | `scene-preview-100k.ply` | `99,966` | `Passed (89.8)` | `sh0-jacobian-covariance-gaussian` | `cpu-exact-back-to-front` | `projectionModel=jacobian-covariance`, `composite=premultiplied-alpha` | `minClipW=0.02`, `offscreenPadding=1.4` |

## First-Party Gaussian Comparison QA

The first-party Gaussian renderer comparison is generated by:

```bash
npm.cmd run qa:first-party-compare
```

The first-party Gaussian camera-edge and near-plane stress QA is generated by:

```bash
npm.cmd run qa:first-party-stress
```

The degraded first-party full-scene source PLY smoke QA is generated by:

```bash
npm.cmd run qa:first-party-full-scene-smoke
```

The first-party default backend readiness assessment is generated by:

```bash
npm.cmd run qa:first-party-readiness
```

The first-party full-scene visual gate assessment is generated by:

```bash
npm.cmd run qa:first-party-full-scene-visual
```

The first-party full-scene performance budget assessment is generated by:

```bash
npm.cmd run qa:first-party-full-scene-performance
```

The runtime Gaussian metadata adapter report is generated by:

```bash
npm.cmd run validate:runtime-metadata
```

The runtime Gaussian format probe report is generated by:

```bash
npm.cmd run validate:runtime-formats
```

The first-party renderable asset resolver report is generated by:

```bash
npm.cmd run validate:renderable-assets
```

The full-scene first-party measurement candidate report is generated by:

```bash
npm.cmd run validate:full-scene-candidate
```

Current comparison result:

| Target | Renderer | Splats | Visual QA |
|---|---|---:|---|
| `ark-gaussian` | `ark-gaussian-webgl2` | `99,966` | `Passed (89.8)` |
| `aholo-adapter` | `aholo` | `99,966` | `Passed (128.9)` |
| `kellogg-independent` | `gaussian-splats-3d` | `99,966` | `Passed (162.7)` |

Primary signature comparison:

| Pair | Signature | Mean Abs RGB | RMS RGB | Similarity |
|---|---|---:|---:|---:|
| `ark-gaussian` vs `aholo-adapter` | Available | `0.9884` | `3.5371` | `0.996124` |

The Kellogg independent baseline is still retained for data and visible-render compatibility. Direct headless canvas signature downsampling returned near-zero contrast for this baseline, so it is not used as a signature-difference gate.

Current stress result:

| Case | Result | Sample Contrast | Signature Contrast | Splats |
|---|---|---:|---:|---:|
| `default-fit` | Passed | `216` | `196` | `99,966` |
| `edge-right` | Passed | `1.78` | `198` | `99,966` |
| `near-plane-close` | Passed | `484.11` | `203` | `99,966` |
| `near-plane-offset` | Passed | `459.67` | `235` | `99,966` |

Current full-scene visual gate result:

| Assessment | Visual Gate | Status | Default Asset | Measurement Candidate | Default Runtime? | Preview First-Party Visual QA |
|---|---|---|---|---|---|---|
| Passed | No | `blocked-before-measurement` | `runtime-sog` | `source-ply` | No | `Passed (89.8)` |

Current full-scene performance budget result:

| Assessment | Performance Gate | Status | Default Asset | Measurement Candidate | Splats | CPU Sort Limit | Ratio |
|---|---|---|---|---|---:|---:|---:|
| Passed | No | `blocked-before-measurement` | `runtime-sog` | `source-ply` | `1,855,266` | `400,000` | `4.638x` |

Current default readiness result:

| Preview Path | Runtime Format Probe | Default Backend Ready | Keep Aholo Default | Blockers |
|---|---|---|---|---:|
| Passed | Passed | No | Yes | `4` |

Readiness blockers:

- manifest default is `runtime-sog`, but the first-party renderer currently accepts direct PLY input only
- manifest default has `1,855,266` splats, above the current `400,000` CPU sort limit
- first-party full-scene visual assessment exists, but the visual gate is still blocked before measurement
- first-party full-scene performance budget assessment exists, but the performance gate is still blocked before measurement

Current runtime metadata result:

| Runtime Asset | Format | Metadata | Direct First-Party Render | Splats | Size |
|---|---|---|---|---:|---:|
| `runtime-sog` | SOG | Ready | No | `1,855,266` | `22,887,509` |
| `runtime-spz` | SPZ | Ready | No | `1,855,266` | `35,364,142` |

Current runtime format probe result:

| Runtime Asset | Container | Probe | Layout | Direct Decode | Bytes / Splat | Source Ratio | Detected Payload |
|---|---|---|---|---|---:|---:|---|
| `runtime-sog` | `sog-zip-webp` | Passed | `meta.json` ready | No | `12.337` | `0.049744` | `7` WEBP channel files |
| `runtime-spz` | `spz-gzip` | Passed | Pending | No | `19.061` | `0.076861` | GZIP payload |

Current SOG layout summary:

| Channel | Files | Metadata |
|---|---|---|
| `means` | `means_l.webp`, `means_u.webp` | mins/maxs available |
| `scales` | `scales.webp` | codebook available |
| `quats` | `quats.webp` | quaternion channel |
| `sh0` | `sh0.webp` | SH0 codebook available |
| `shN` | `shN_centroids.webp`, `shN_labels.webp` | `bands=3`, `count=65,536` |

Current renderable asset resolver result:

| Requested | Renderable | Mode | Direct | Degraded |
|---|---|---|---|---|
| `runtime-sog` | `preview-ply` | `preview-substitute` | No | Yes |
| `runtime-spz` | `preview-ply` | `preview-substitute` | No | Yes |
| `preview-ply` | `preview-ply` | `direct` | Yes | No |
| `source-ply` | `source-ply` | `direct` | Yes | No |

Current full-scene measurement candidate result:

| Requested | Candidate | Mode | First-Party Loadable | Default Runtime Direct? | Splat Equivalent |
|---|---|---|---|---|---|
| `runtime-sog` | `source-ply` | `source-ply-substitute` | Yes | No | Yes |
| `runtime-spz` | `source-ply` | `source-ply-substitute` | Yes | No | Yes |
| `source-ply` | `source-ply` | `direct-default` | Yes | Yes | Yes |

Previous source PLY full-scene smoke result before Round 3 sorting repair:

| Asset | Result | Renderer | Decoded Splats | Rendered Splats | Invalid Skipped | Sorting | LOD | Visual QA | Settle Frames | Duration |
|---|---|---|---:|---:|---:|---|---|---|---:|---:|
| `source-ply` | Passed | `ark-gaussian-webgl2` | `1,854,627` | `1,854,627` | `639` | `source-order` | disabled, `full-density-source-order` | `Passed (202.7)` | `1` | `83.376s` |

Round 4 source PLY full-scene smoke result:

| Asset | Result | Renderer | Decoded Splats | Rendered Splats | Invalid Skipped | Sorting | Projection / Composite | LOD | Visual QA | Duration |
|---|---|---|---:|---:|---:|---|---|---|---|---:|
| `source-ply` | Passed | `ark-gaussian-webgl2` | `1,854,627` | `1,854,627` | `639` | `cpu-bucket-back-to-front` | `jacobian-covariance` / `premultiplied-alpha` | disabled, `full-density-bucket-depth-sort` | `Passed (222.8)` | `76.476s` |

Round 5 source PLY full-scene smoke result:

| Asset | Result | Renderer | Decoded Splats | Rendered Splats | Invalid Skipped | Coverage | Shading | Source / Render SH | Visual QA | Duration |
|---|---|---|---:|---:|---:|---:|---|---|---|---:|
| `source-ply` | Passed | `ark-gaussian-webgl2` | `1,854,627` | `1,854,627` | `639` | `99.97%` | `sh1-view-dependent` | `3 / 1` | `Passed (223.7)` | `71.769s` |

Current source PLY timing breakdown:

| Read | Decode | Pack | Upload | Renderer Load | Visual Gate | Load Peak |
|---:|---:|---:|---:|---:|---:|---:|
| `1,444.5ms` | `1,388.4ms` | `197.6ms` | `84.0ms` | `3,117ms` | `48,492ms` | `948.184MiB` |

This confirms the invalid-splat policy on the real full source PLY: the first-party decoder skips the `639` invalid positions and preserves `1,854,627` valid decoded splats. The smoke renderer now draws all decoded source splats for this scene. Round 3 replaced the previous `source-order` blending fallback with `cpu-bucket-back-to-front` sorting for source PLY scenes up to `2,000,000` splats. Round 4 replaced the simplified axis-endpoint projection with Jacobian covariance projection and premultiplied alpha. This remains a degraded measurement candidate rather than proof that the full default runtime is production-ready.

Important current SH limitation:

> The source PLY is SH degree `3`, but `ark-gaussian` currently renders only
> `renderShDegree=1` (`sh1-view-dependent`). Full SH3 color evaluation and
> GPU-friendly SH packing remain first-party visual parity blockers.

Current same-camera SH3 diagnostic:

| Pair | Asset | Mean Abs RGB | RMS RGB | Similarity | Interpretation |
|---|---|---:|---:|---:|---|
| ARK SH1 vs Aholo SH0 | `source-ply` | `1.6336` | `7.5424` | `0.993594` | Current source baseline |
| ARK SH1 vs Aholo SH3 | `source-ply` | `1.6483` | `7.7097` | `0.993536` | SH3 reference does not reduce ARK delta |
| Aholo SH0 vs Aholo SH3 | `source-ply` | `0.1345` | `1.1341` | `0.999473` | SH3 color effect is small in this camera |
| Aholo SH0 vs Aholo SH3 | `preview-ply` | `0.0518` | `0.6282` | `0.999797` | Preview confirms small SH3 delta |

This means full SH3 is still required for format completeness, but it is not the
current leading explanation for the remaining first-party visual delta.

Current same-camera pipeline isolation:

| Pair | Asset | Mean Abs RGB | RMS RGB | Similarity | Interpretation |
|---|---|---:|---:|---:|---|
| ARK default vs Aholo SH0 | `preview-ply` | `0.9728` | `3.5066` | `0.996185` | Preview baseline |
| ARK source-order vs Aholo SH0 | `preview-ply` | `0.9758` | `3.5421` | `0.996173` | Sorting override is slightly worse |
| ARK straight alpha vs Aholo SH0 | `preview-ply` | `0.9728` | `3.5066` | `0.996185` | Blend override is identical to default |
| ARK no-preblur vs Aholo SH0 | `preview-ply` | `1.0002` | `3.7612` | `0.996078` | Projection shortcut is worse |
| ARK unit-focal vs Aholo SH0 | `preview-ply` | `0.9985` | `3.5961` | `0.996084` | Projection shortcut is worse |
| ARK compact-kernel vs Aholo SH0 | `preview-ply` | `1.0090` | `3.8946` | `0.996043` | Projection shortcut is worse |
| ARK default vs Aholo SH0 | `source-ply` | `1.6336` | `7.5424` | `0.993594` | Source baseline |
| ARK source-order vs Aholo SH0 | `source-ply` | `1.6602` | `7.8448` | `0.993489` | Sorting override is worse |
| ARK straight alpha vs Aholo SH0 | `source-ply` | `1.6336` | `7.5424` | `0.993594` | Blend override is identical to default |

This isolates the current renderer gap away from splat count, source-order
sorting, and the tested alpha blend function. Projection remains sensitive, but
the tested parameter shortcuts did not improve parity.

Important current asset coverage limitation:

> `preview-ply` is only a diagnostic preview asset. It contains `99,966` splats,
> or `5.39%` of the source manifest splat count. Use `source-ply` for full
> source-density first-party renderer checks on this machine.

Important renderer requirements:

> The first-party renderer must skip or repair invalid splats before computing bounds, sorting, packing, or rendering.

> The first-party renderer must expose clipping state in QA debug output, and renderer QA must fail when that clipping state is missing or disabled.

## Invalid Splat Policy

`decodeGaussianPly()` now has an explicit invalid position policy:

| Policy | Behavior |
|---|---|
| `skip` | Default. Invalid positions are excluded from decoded arrays. |
| `keep` | Invalid source rows remain in decoded arrays. This is for debugging only. |
| `error` | Decode fails if any invalid position exists. |

`ArkGaussianData` preserves traceability:

| Field | Meaning |
|---|---|
| `sourceCount` | Original source splat count. |
| `count` | Decoded output splat count after policy/limit. |
| `sourceIndices` | Source row index for every decoded splat. |
| `invalidSourceIndices` | Source rows with invalid positions. |

For the current large asset, default decode should eventually output `1,854,627` renderable splats from `1,855,266` source splats.

Regression fixture expected behavior:

```json
{
  "sourceCount": 4,
  "count": 3,
  "sourceIndices": [0, 2, 3],
  "invalidSourceIndices": [1]
}
```

## SDK Types

Browser-side TypeScript contract:

```text
src/sdk/gaussian/types.ts
src/sdk/gaussian/ply.ts
src/sdk/gaussian/runtimeMetadata.ts
src/sdk/gaussian/renderableAsset.ts
src/sdk/gaussian/fullSceneCandidate.ts
```

Primary types:

- `ArkGaussianPlyHeader`
- `ArkGaussianSummary`
- `ArkGaussianData`
- `ArkGaussianRuntimeMetadata`
- `ArkFirstPartyRenderableAssetResolution`
- `ArkFirstPartyFullSceneCandidateResolution`

Primary functions:

- `parseGaussianPlyHeader`
- `summarizeGaussianPly`
- `decodeGaussianPly`
- `buildGaussianRuntimeMetadata`
- `resolveFirstPartyRenderableAsset`
- `resolveFirstPartyFullSceneCandidate`

## Next Data Contract Work

- Persist visual QA outputs into versioned scene reports.
- Move first-party renderer sorting from CPU main-thread sort toward worker or GPU-assisted sorting after the projection and packed data path is stable.
- Add SOG/SPZ direct loading or a first-party runtime conversion path.
- Use the runtime format probe as the next decoder entry point: SOG exposes WEBP channels and a summarized `meta.json` layout; SPZ is currently only identified as GZIP.
- Replace degraded stride LOD with a production large-scene strategy: direct SOG/SPZ loading, worker/GPU sorting, chunk streaming, or view-dependent LOD with quality comparison.
- Convert the full-scene visual and performance assessments into measured gates before changing the default backend.
